#-----------------------------------------------------------------------------
#  опирование строки в другую
# ¬ макроопределение передаютс€ адреса строк
.macro strncpy (%dest, %source, %num)
	la a0 %dest
	la a1 %source
	mv a2 %num
	jal strncpy
.end_macro

